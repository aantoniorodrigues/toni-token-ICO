App = {
    // Initializes the app.
    init: function() {
        console.log('App initialized...');
    }

    // Instantiates web3.
}

// Initialize the app.
$(function() {
    $(window).load(() => {
        App.init();
    })
});